{
  "name": "ht-export",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "export": "tsc && node dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "firebase": "^10.0.0"
  }
}
